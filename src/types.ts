export type TTypeName = "Icon" | "Background";

export type TIcon = {
  name: string;
  icon: string;
  iconSize: number;
  iconFillColor: string;
  iconStrokeWidth: number;
  iconStrokeColor: string;
  iconRotation: number;
  bgSize: number;
  bgColor: string;
  bgRadius: number;
};
